# iOS アプリ 画面設計（MVP）

作成日: 2026-07-14
関連: [MVP 実装プラン](../plans/archive/mvp-morning-briefing.md) / [機能アイデア](app-features.md)
モックアップ（Artifact）:
- **4タブ構成（HOME / GitHub / Calendar / Setting）← 最新**: https://claude.ai/code/artifact/bcf1b560-caf3-451d-af1d-4c249c2bfe2e
- レイアウト3案の比較: https://claude.ai/code/artifact/b900f92f-3430-4439-b243-e383235600ab
- 初期4画面（オンボーディング/通知/ブリーフィング/設定）: https://claude.ai/code/artifact/4c8dc1f9-e4f3-4e8c-8360-13a43a894b13

## ナビゲーション構成の決定（2026-07-14 更新）

**下部ナビ4タブ: HOME / GitHub / Calendar / Setting**（ユーザー決定）。
HOME に **案A「統合フィード（優先順1画面）」** を据える。案B/Cは見送り。

- **HOME** = 横断ダイジェスト（案A）。要対応・締切・TODOサマリー の要点を優先順に。
  各セクション見出しは対応タブへのリンク（「GitHub ›」「Calendar ›」）。
  「昨日の GitHub」は GitHub タブへ集約済み（2026-07-17 削除）
- **GitHub** = 更新順リポジトリ一覧 → タップでリポジトリ詳細（2026-07-17 刷新 [plan](../plans/archive/github-tab-repo-list.md)）
  - 一覧: 直近 90 日に push があった非 fork・非 archived リポジトリ最大 20 件を pushed_at 降順。
    行 = リポジトリ名 + 相対更新時刻 + TODO 件数ピル + 直近作業サマリー（LLM 1〜2 文）+ TODO サマリー
  - 詳細: ヘッダ（owner/name + GitHub リンク）/ TODO（サマリー + 全タスク）/ 直近の作業（サマリー +
    コミット最大 10 件）/ 昨日の活動（commits/PR）。TODO・コミットは 5 件超を折りたたみ
  - `payload.repos` が無い旧ブリーフィング表示中は従来の 2 セクション（昨日の活動 + TODO 全文）に
    フォールバック。放置検知は後フェーズ
- **Calendar** = 週ストリップ＋アジェンダ。週/月グリッド・選択日は Google 予定 + 締切を統合表示。
  一覧は「今後の締切」（Canvas のみ）と「カレンダー（直近7日）」（Google 終日予定）の別グループ（2026-07-17 変更）
- **Setting** = 通知・連携ソース・`TODO.md` 読み取り対象リポジトリ

役割分担: **要点は HOME、全量は各タブ**（同じデータの見せ方を変えるだけ。二重に作り込まない）。
毎朝の push は HOME の要約を届け、タップで HOME を開く。タブはプル側。

これに伴い **GitHub の作業把握を MVP に取り込む**（下記「よく使う機能」）。元計画では Phase 5 だが前倒し。

### よく使う機能（この設計が満たすもの）

1. 朝に **今日の作業と直近の締切** を確認する
2. **GitHub の前日までの作業** と **今後の作業（各リポジトリの `TODO.md`）** を確認する

## スコープ

MVP の画面 = **オンボーディング + ロック画面通知 + 4タブ本体（HOME / GitHub / Calendar / Setting）**。
初回の通知許可 → 毎朝の受け取り → HOME で確認 → 各タブで深掘り、を通す。
実装は **HOME を先に動かし、GitHub / Calendar タブは順に肉付け**する段取り（[MVP プラン](../plans/archive/mvp-morning-briefing.md)）。
ウィジェット / Live Activities / チャットは後フェーズ（[spec Phase 3+](app-features.md)）。

## 画面

### 1. オンボーディング（初回起動）
- 目的: 通知を許可し、デバイストークンを backend（`POST /devices`）に登録する
- 内容: アプリの説明を「カレンダー / Canvas 締切 / 要対応メール」の3点に絞る
- ボタン: 「通知を許可して始める」/「あとで」
- 許可が下りたら APNs のデバイストークンを取得 → backend に送信

### 2. ロック画面通知（毎朝 07:00 PT）
- **主役のチャネル。** v1 は要点をフル本文で push（[ペイロード方式1](../plans/archive/mvp-morning-briefing.md)）
- 文面例: 「タイムスケジュールの予定はなし。締切が2日後、要対応メール2件。Google One が7/19に終了します。」
- タップでブリーフィング画面（3）へ

### 3. 今日のブリーフィング（メイン画面）＝ 案A 統合フィード
- 構成: **LLM が書いた要約（上）＋ 緊急順のドメイン別セクション（下）を1画面スクロール**
- 大タイトル = 日付、サブ = あいさつ
- 「今日のまとめ」カード: LLM の日本語プロース要約（締切・GitHub・タスクを1〜2文で）
- セクション（緊急順、iOS グループ化リスト）:
  - **要対応**（Gmail トリアージ）: 済ワンタップ。LLM がアーカイブ済み含む受信メールから
    返信・対応が必要なものを判定（2026-07-17 変更。従来の in:inbox 収集は受信即アーカイブ運用で
    常に 0 件だったため）。最重要のため最上部（2026-07-17 移動）
  - **締切が近い**（Canvas iCal のみ、14 日先まで収集。2026-07-17 変更）: 残日数を赤ピル（例「あと2日」）
  - **カレンダー（直近7日）**（Google カレンダーの終日予定、7 日以内のみ。空なら非表示）: 締切とは別グループ
  - **GitHub**（各リポジトリの `TODO.md` の LLM サマリー。2026-07-17 変更）: リポジトリごとに
    タグ + 件数 + 日本語プロース 1〜2 文。リポジトリの TODO 内容が前回と同じなら backend が
    キャッシュを返す（LLM 呼び出しなし）。サマリーが無いリポジトリ（旧 payload / 生成失敗）は
    件数のみにフォールバック。リポジトリは 5 件まで表示し、6 件目以降は
    「残り N 件を表示」トグルで展開（展開後は「折りたたむ」で戻せる）。全量は GitHub タブ
  - 「昨日の GitHub」（commits/PR 集計）は 2026-07-17 に HOME から削除。
    同等の情報は GitHub タブ（リポジトリ一覧・詳細）で確認する
    （[plan](../plans/archive/remove-home-github-sections.md)）
- フッター: 「07:00 に自動更新 / 更新↻」（プル）
- 表示の作法: リポジトリ名は等幅・開発系は青、締切は赤ピル

### 4. 設定
- 通知: ブリーフィング ON/OFF、配信時刻（既定 7:00 AM）、タイムゾーン（Seattle）、言語（日本語）
- 連携ソース: Google カレンダー / Gmail / Canvas(iCal) の接続状態
- トリアージ: 要対応の判定基準、自動配信メールの除外トグル
- **原則**: マスターデータの編集は持たない。設定は最小限（[spec 2-5](app-features.md)）

## デザイン方針

- **配色**: 夜明けの光をモチーフ。AIの定番（クリーム×テラコッタ）を避け、クールなスレート地 × 琥珀アクセント。締切=赤 / 完了=緑 のセマンティックカラーはアクセントと分離
- **タイポ**: 日本語を含むため Web フォントは使わず **iOS システムスタック（SF Pro / Hiragino）** を意図的に採用。実機と同じ見え方。階層はウェイト・サイズ・数字の等幅（tabular-nums）で作る
- **情報設計**: 要約を上・詳細を下。状態を形で（残日数ピル / 済チェック / 接続バッジ）。締切の逼迫は色で一目化
- **ライト / ダーク両対応**

## この設計から実装 Step 7 への含意

MVP の iOS アプリ（[Step 7](../plans/archive/mvp-morning-briefing.md)）が持つ画面はこの4つ。
バックエンド API との対応:
- オンボーディング → `POST /devices`
- ブリーフィング表示・更新↻ → `GET /briefings/latest`
- 通知本文 → APNs ペイロード（v1 フル内容）

## 未確定

- 要対応の「済/後で/無視」操作を通知上でも行うか（アクション付き通知）は MVP 後
- 要約カードの文量・トーンは実データで調整
- アプリアイコンの正式デザイン（モックはサンライズの暫定マーク）
